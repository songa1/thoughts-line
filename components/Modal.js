import { useEffect, useState } from "react";
const Modal = (props) => {
    const {isShown, setIsShown} = props;

   
  const closeModal = () => {
    setIsShown(false);
  };

    return (
        <div>
            
        </div>
    )
}

export default Modal;